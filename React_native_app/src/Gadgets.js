import React from 'react'
import { SafeAreaView, Text, View } from 'react-native'
export const GadgetScreen = ({navigation}) => {
    return (<SafeAreaView>
<View>
        <Text>This is gadgets screen</Text>
    </View>
    </SafeAreaView>
    );
}