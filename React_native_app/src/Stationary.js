import React from 'react'
import { SafeAreaView, Text, View } from 'react-native'
export const StationaryScreen = ({navigation}) => {
    return (<SafeAreaView>
<View>
        <Text>This is Stationary screen</Text>
    </View>
    </SafeAreaView>
    );
}