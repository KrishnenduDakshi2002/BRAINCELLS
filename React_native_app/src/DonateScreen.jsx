import React from 'react';

import { View } from 'react-native';

import Header from './components/Header';

const DonateScreen = ({ params }) => (
	<View style={{ marginTop: 30 }}>
		<Header title="Donate item" />
	</View>
);

export default DonateScreen;
